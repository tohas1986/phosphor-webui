<loader loading="loading"></loader>
<div id="fans" class="fans">
  <h1>Fans</h1>
  <div class="page-header">
    <h2 class="inline">All fans present in the system</h2>
      <a ng-href="data:text/json;charset=utf-8,{{export_data}}" class="btn btn-tertiary float-right" download="{{export_name}}"><icon file="icon-export.svg"></icon>Export</a>
  </div>

  <div>
    <p class="content-label" aria-label="fans filter">Filter fans</p>
    <div class="content__search">
      <label for="content__search-input">Fans Search</label>
      <input id="content__search-input" type="text" ng-model="customSearch" ng-keydown="doSearchOnEnter($event)"/>
      <div class="search-submit__wrapper">
        <button class="clear-input" ng-click="clear()">&#10005;</button>
        <input id="content__search-submit" type="submit" class="btn btn-primary content__search-submit" value="Filter" ng-click="doSearchOnClick()"/>
      </div>
    </div>

    <div class="toggle-filter">
      <p class="content-label">FILTER BY SEVERITY</p>
      <button class="btn" ng-click="toggleSeverityAll()"
          ng-class="selectedSeverity.all ? 'btn-primary' : 'btn-secondary'">All
      </button>
      <button class="btn" ng-click="toggleSeverity('critical')"
          ng-class="selectedSeverity.critical ? 'btn-primary' : 'btn-secondary'">Critical
      </button>
      <button class="btn" ng-click="toggleSeverity('warning')"
          ng-class="selectedSeverity.warning ? 'btn-primary' : 'btn-secondary'">Warning
      </button>
      <button class="btn" ng-click="toggleSeverity('normal')"
          ng-class="selectedSeverity.normal ? 'btn-primary' : 'btn-secondary'">Normal
      </button>
    </div>
  </div>

  <div ng-show="filteredFanData.length == 0">
    <span ng-if="selectedSeverity.all">{{messages.NO_SENSOR_DATA}}</span>
    <span ng-if="selectedSeverity.critical">{{messages.CRITICAL_NO_SENSOR_DATA}}</span>
    <span ng-if="selectedSeverity.warning">{{messages.WARNING_NO_SENSOR_DATA}}</span>
    <span ng-if="selectedSeverity.normal">{{messages.NORMAL_NO_SENSOR_DATA}}</span>
  </div>

  <table id="fan-categories" class="fans__table" cellpadding="0" cellspacing="0" ng-show="filteredFanData.length">
    <thead class="fans__thead fixed-table-header">
      <tr class="fans__thead-row">
        <th class="fans__thead-cell">Fans ({{filteredFanData.length}})<th>
        <th class="fans__thead-cell">Low critical<th>
        <th class="fans__thead-cell">Low warning<th>
        <th class="fans__thead-cell fan__heading-current">Current<th>
        <th class="fans__thead-cell">High warning<th>
        <th class="fans__thead-cell">High critical<th>
      </tr>
    </thead>
    <tbody class="fans__tbody">
      <tr class="fans__tbody-row" ng-repeat="fan in data|filter:filterBySeverity|filter:filterBySearchTerms|orderBy:'+custom_order' as filteredFanData">
        <th class="fans__tbody-header">
          <span class="icon" ng-class="{'icon__critical': fan.status == 'critical', 'icon__warning': fan.status == 'warning', 'icon__normal': fan.status == 'normal'}" aria-label="Fan status: {{fan.status}}" ></span>
          <span>{{fan.title}}</span>
        </th>
        <td class="fans__tbody-cell">
          <span class="fans__tbody-cell__title">Low critical</span>
          <span class="fans__tbody-cell__content">
              <span class="fans__tbody-info">{{fan.CriticalLow}}</span>
              <span class="fans__tbody-degree" ng-if="fan.unit == 'C'">&deg;</span>
            <span class="fans__tbody-unit">{{fan.unit}}</span>
          </span>
        </td>
        <td class="fans__tbody-cell">
          <span class="fans__tbody-cell__title">Low warning</span>
          <span class="fans__tbody-cell__content">
              <span class="fans__tbody-info">{{fan.WarningLow}}</span>
              <span class="fans__tbody-degree" ng-if="fan.unit == 'C'">&deg;</span>
            <span class="fans__tbody-unit">{{fan.unit}}</span>
          </span>
        </td>
        <td class="fans__tbody-cell fans__tbody-current" ng-class="{'fans__tbody-current--critical': fan.status == 'critical', 'fans__tbody-current--warn': fan.status == 'warning', 'fans__tbody-current--normal': fan.status == 'normal'}">
          <span class="fans__tbody-cell__title">Current</span>
          <span class="fans__tbody-cell__content">
              <span class="fans__tbody-info">{{fan.Value}}</span>
              <span class="fans__tbody-degree" ng-if="fan.unit == 'C'">&deg;</span>
            <span class="fans__tbody-unit">{{fan.unit}}</span>
          </span>
        </td>
        <td class="fans__tbody-cell">
          <span class="fans__tbody-cell__title">High warning</span>
          <span class="fans__tbody-cell__content">
            <span class="fans__tbody-info">{{fan.WarningHigh}}</span>
            <span class="fans__tbody-degree" ng-if="fan.unit == 'C'">&deg;</span>
            <span class="fans__tbody-unit">{{fan.unit}}</span>
          </span>
        </td>
        <td class="fans__tbody-cell">
          <span class="fans__tbody-cell__title">High critical</span>
          <span class="fans__tbody-cell__content">
            <span class="fans__tbody-info">{{fan.CriticalHigh}}</span>
            <span class="fans__tbody-degree" ng-if="fan.unit == 'C'">&deg;</span>
            <span class="fans__tbody-unit">{{fan.unit}}</span>
          </span>
        </td>
      </tr>
    </tbody>
  </table>
</div>
